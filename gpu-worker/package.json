{
  "name": "gpu-worker",
  "version": "1.0.0",
  "description": "Pulls upscale jobs off the Redis queue and runs Real-ESRGAN on the GPU node",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "redis": "^3.1.2"
  }
}
